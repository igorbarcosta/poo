# Aula 05 Phase E behavioral observations

These observations are judgment-based and are intentionally separate from
deterministic workflow evidence.

## Semantic parity

The candidate preserves the approved semantic core: incomplete object
creation; Java default values versus domain-valid state; constructor purpose
and basic syntax; argument → parameter → field; the narrow use of `this`;
`ItemPedido`; numeric initial-state invariants with zero accepted; an optional,
non-implemented `Reserva` transfer; and the bridge to Lab 05. No advanced
constructor features, exception policy, textual-description validation,
complete `Reserva`, `Pedido`, or collections were invented.

Meaningful concepts classify as follows:

- `PRESERVED`: incomplete creation, default state/domain state, constructor,
  basic syntax, value flow, `this`, `ItemPedido`, invariants, accepted zero,
  class responsibility, Lab 05 bridge;
- `CHANGED_ACCEPTABLY`: wording, consolidation of some answer frames, 45 → 38
  frames, and explicit presentation of deliberate exclusions;
- `MISSING`: no canonical concept is wholly absent;
- `INVENTED`: no material semantic scope was added.

## Pedagogical parity and reduction quality

The problem-first three-block progression and the broad prediction → contrast
→ formalization rhythm survive. The reduction removes repetition and makes
the accepted-zero boundary unusually clear. It also removes some cognitive
beats: most importantly, the visible resolution of the `this` activity and
context needed by the responsibility and `Reserva` transfers. The result is a
meaningful independent derivation, but not yet strong enough to recommend
behavioral equivalence without correction.

## POO identity and course policy

Portuguese, Java-as-support, responsibility/state as the conceptual axis,
Project 1’s `ItemPedido`, the three Aula 05 block boundaries, current six-pause
grammar, local Marp classes, presenter notes, 90-minute context, elastic
transfer, and Aula → Lab relationship remain visible. No Docemas generic
artifact acquired POO, Java, Marp, renderer, or publication policy.

## Apparent improvements

- Clearer explicit statement that zero remains accepted.
- Concise boundary against premature exceptions and advanced validation.
- Reduced expository repetition while retaining all canonical concepts.
- Explicit current approval and provenance make authority traceable without
  turning the historical deck into truth.

## Apparent regressions

- The `descricao = descricao;` activity lacks a projected resolution.
- The responsibility comparison lacks its concrete client/data premise.
- The `Reserva` transfer does not incorporate the contextual clarification
  requested by the current retrospective.
- Fewer active prediction/answer beats may reduce classroom processing time.

## Human judgment required

- Is the 38-frame sequencing preferable to the 45-frame reference?
- Is the reduced number of prediction/answer frames acceptable?
- Should `Reserva` be contextualized and retained, or shortened as an elastic
  deepening?
- Does the wrapper expose enough of the POO pedagogy while delegating generic
  mechanics clearly?
- After a corrected candidate demonstrates parity, which confirmed generic
  duplicates may be removed while retaining local entrypoints and policy?
