# Aula 05 historical/candidate-v2 difference inventory

The approved `LessonDesign + Lesson` pair remains semantic authority. The
historical deck is parity reference evidence only. Candidate v2 is the proposed
parity candidate; candidate v1 and its first review remain preserved evidence
of the first derivation.

## Preserved candidate-v1 history

Candidate v1 had 38 frames and projection readiness
`CORRECTIONS_RECOMMENDED`. Its material findings remain recorded, without
rewriting their history, in `projection-review.md`:

- absent projected closure after `descricao = descricao;`;
- missing concrete client/data premise in the responsibility activity;
- missing domain/argument context in the optional `Reserva` transfer.

## Candidate-v2 inventory

| Difference | Classification | Evidence and assessment |
| --- | --- | --- |
| Candidate v2 uses 40 frames versus 45 historical frames and 38 in v1 | `PEDAGOGICALLY_EQUIVALENT`, `NEEDS_HUMAN_JUDGMENT` | Count is evidence only. V2 retains the three-block causal trajectory, restores two missing contextual/closure beats, and still omits historical repetition that is not required by the canonical pair. |
| Opening statement uses a domain/Java contrast before the three trajectory questions | `SEMANTIC_EQUIVALENT`, `PEDAGOGICALLY_EQUIVALENT` | It establishes incomplete creation as the organizing problem without changing the canonical question or scope. |
| Dedicated “quando o item fica pronto?” historical frame remains folded into the incomplete-item activity | `SEMANTIC_EQUIVALENT`, `POTENTIAL_IMPROVEMENT`, `NEEDS_HUMAN_JUDGMENT` | Prediction, state comparison, and the risk of early circulation remain; the reduction avoids a repeated framing beat. |
| `this` is introduced before the check, then explicitly resolved after the check | `SEMANTIC_EQUIVALENT`, `PEDAGOGICALLY_EQUIVALENT` | V2 preserves the candidate ordering but now completes prediction → hypothesis → contrast → visible resolution. The new frame states why self-assignment misses the field and limits `this` to disambiguation. |
| Numeric invalid-state prediction/answer pair is condensed into explanation | `SEMANTIC_EQUIVALENT`, `NEEDS_HUMAN_JUDGMENT` | Rules, negative-input consequence, accepted zero, defaults, and policy limits remain projected. The active prediction beat is reduced, but no approved meaning is lost. |
| Responsibility comparison is spread across a concrete-premise frame and a contrast frame | `SEMANTIC_EQUIVALENT`, `PEDAGOGICALLY_EQUIVALENT`, `POTENTIAL_IMPROVEMENT` | V2 defines client, identifies the received data, shows full external preparation, and then contrasts the invariant-preserving constructor. This resolves v1 PR-02 without adding error policy. |
| `Reserva` uses a named lodging scene and named arguments instead of unexplained literals | `SEMANTIC_EQUIVALENT`, `POO_POLICY_PRESERVED`, `POTENTIAL_IMPROVEMENT` | Context supports transfer of the same creation-responsibility principle. It remains optional, elastic, conceptual, and not implemented; no new invariant is asserted. |
| Candidate v2 explicitly states that zero remains accepted | `SEMANTIC_EQUIVALENT`, `POTENTIAL_IMPROVEMENT` | This makes the approved canonical boundary visible and avoids inventing stricter invariants. |
| Candidate v2 explicitly lists excluded error policies in a `trap` frame | `POO_POLICY_PRESERVED`, `POTENTIAL_IMPROVEMENT` | Java remains subordinate to the current POO problem; exceptions and text validation are not introduced. |
| Candidate v2 uses only current pause names and POO utility classes | `POO_POLICY_PRESERVED` | Source scan retains `activity`, `concept-key`, `java-focus`, `tip`, `trap`, and `synthesis`, Marp front matter, `theme: poo`, and block markers `5.1`, `5.2`, `5.3`. |
| Candidate v2 has independent bytes and separately current Docemas provenance | `POO_POLICY_PRESERVED`, `POTENTIAL_IMPROVEMENT` | V1 and historical bytes remain unchanged. V2 binds the same approved pair and `poo-marp-v1` profile through deterministic provenance. |
| Rendered geometry and visual readability have not been tested | `NEEDS_HUMAN_JUDGMENT` | Rendering is explicitly prohibited in this phase; visual questions remain `RENDER_VALIDATION_REQUIRED_LATER` and do not alter the source-level readiness result. |

No `POTENTIAL_REGRESSION` remains after the bounded source correction. Cosmetic
wording and comment differences are intentionally omitted unless they change a
cognitive operation or consumer policy.
