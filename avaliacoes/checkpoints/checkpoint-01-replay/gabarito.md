# Gabarito

## Variante A

### Q01

#### a)

`true`

#### b)

`false`

#### c)

`true`

#### d)

`10.0`

#### e)

`8.0`

#### f)

`12.0`

#### g)

`1.0`

### Q02

#### a)

V.

#### b)

V.

#### c)

V.

#### d)

V.

#### e)

F. Um método acessa os atributos do próprio objeto sem precisar recebê-los como parâmetros.

### Q03

B. O método usa `totalMilimetros` do próprio objeto e recebe apenas o limite necessário à comparação.

### Q04

D. Sem modificador, o atributo possui acesso de pacote e pode ser acessado por outra classe do mesmo pacote.

### Q05

C. As duas variáveis continuam apontando para o mesmo objeto; `-2.0` não altera o total, e os registros válidos produzem `10.0`.

### Q06

A. `definirTotal` permite substituir o estado sem passar pela validação de `registrarChuva`.

## Variante B

### Q01

#### a)

`false`

#### b)

`true`

#### c)

`false`

#### d)

`6.0`

#### e)

`9.0`

#### f)

`11.0`

#### g)

`2.0`

### Q02

#### a)

F. Atributos representam estado; métodos representam comportamento.

#### b)

F. O parâmetro existe apenas durante a chamada e não se torna atributo do objeto.

#### c)

F. Um método `void` pode alterar o estado do objeto.

#### d)

F. Alterar a variável que recebeu o `double` não modifica o atributo do objeto.

#### e)

V.

### Q03

E. O método usa `totalMilimetros` do próprio objeto e recebe apenas o limite necessário à comparação.

### Q04

B. Sem modificador, o atributo possui acesso de pacote e pode ser acessado por outra classe do mesmo pacote.

### Q05

D. As duas variáveis continuam apontando para o mesmo objeto; `-3.0` não altera o total, e os registros válidos produzem `12.0`.

### Q06

C. `definirTotal` permite substituir o estado sem passar pela validação de `registrarChuva`.
